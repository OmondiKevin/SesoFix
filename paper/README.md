# Paper workspace

`PAPER_OUTLINE.md` is the active structure for the new paper. It contains no
invented results and will be populated only from frozen experiment artifacts.

`legacy/` preserves the former manuscript, reviews, plans, and scripts for
forensic history. Nothing in `legacy/` is citable evidence for the new paper.
